server {
    listen 80;
    server_name exemplehotel.tw1.ru www.exemplehotel.tw1.ru;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    server_name exemplehotel.tw1.ru www.exemplehotel.tw1.ru;

    ssl_certificate /etc/letsencrypt/live/exemplehotel.tw1.ru/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/exemplehotel.tw1.ru/privkey.pem;

    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;

    location / {
        root /opt/glamping/glamping-frontend/apps/guest/dist;
        try_files $uri $uri/ /index.html;
    }

    location /admin {
        alias /opt/glamping/glamping-frontend/apps/admin/dist;
        try_files $uri $uri/ /admin/index.html;
    }

    # API
    location /api {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }

    # WebSocket
    location /socket.io {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}
